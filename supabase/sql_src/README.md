# SQL sources

Maintain schema and function definitions in this directory. Migrations are immutable deployment
artifacts created with `supabase migration new`; update both the relevant source file and the new
migration in the same change.
